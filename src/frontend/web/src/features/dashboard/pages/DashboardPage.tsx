import { PageHeader } from "@/shared/ui/PageHeader";

export function DashboardPage() {
  return (
    <PageHeader
      titleKey="nav.dashboard"
      docsRef="AS-009: landing page với số đếm cơ bản (Scope Breakdown)"
    />
  );
}
