import { PageHeader } from "@/shared/ui/PageHeader";

export function AnnotationWorkspacePage() {
  return (
    <PageHeader
      titleKey="pages.annotationWorkspace"
      docsRef="AC mục 6/7, VR-ANN-*, Screen Spec màn 2 (answer context + 6 dim + source viewer)"
    />
  );
}
