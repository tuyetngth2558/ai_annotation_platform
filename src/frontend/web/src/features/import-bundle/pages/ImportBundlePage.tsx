import { PageHeader } from "@/shared/ui/PageHeader";

export function ImportBundlePage() {
  return (
    <PageHeader
      titleKey="nav.import"
      docsRef="Import schema §2, VR-UP-*, Screen Spec màn 1.2 (upload + validate + parse preview)"
    />
  );
}
