import { PageHeader } from "@/shared/ui/PageHeader";

export function QaReviewWorkspacePage() {
  return (
    <PageHeader
      titleKey="pages.qaReview"
      docsRef="AC mục 8, VR-QA-*, Screen Spec màn 3 (diff view + approve/return)"
    />
  );
}
