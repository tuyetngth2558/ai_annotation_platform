import { PageHeader } from "@/shared/ui/PageHeader";

export function AuditLogPage() {
  return (
    <PageHeader
      titleKey="nav.audit"
      docsRef="AC mục 10, BR-10.1 INSERT-only. Chỉ ADMIN (AC-10.2)"
    />
  );
}
