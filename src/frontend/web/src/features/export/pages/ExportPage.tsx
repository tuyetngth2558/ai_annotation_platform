import { PageHeader } from "@/shared/ui/PageHeader";

export function ExportPage() {
  return (
    <PageHeader
      titleKey="nav.export"
      docsRef="AC mục 9, Import schema §10 (CSV claim-level, chỉ Approved — BR-9.1)"
    />
  );
}
