import { PageHeader } from "@/shared/ui/PageHeader";

export function ProjectListPage() {
  return (
    <PageHeader
      titleKey="nav.projects"
      docsRef="AC-1.4 danh sách project + trạng thái LLM. Screen Spec màn 1"
    />
  );
}
