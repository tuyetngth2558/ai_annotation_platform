import { PageHeader } from "@/shared/ui/PageHeader";

export function ProjectSetupPage() {
  return (
    <PageHeader
      titleKey="pages.projectSetup"
      docsRef="AC-1.1..1.3: tạo project, cấu hình LLM (key encrypt — BR-1.2), gán nhân sự"
    />
  );
}
