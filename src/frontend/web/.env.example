# Backend API base URL. Use a full URL in local split-stack development.
# Default in code: /api/v1
VITE_API_BASE_URL=http://localhost:8000/api/v1

# Demo auth fallback (login thuần FE khi BE lỗi). false = chỉ dùng login thật từ DB.
VITE_DEMO_AUTH=false

